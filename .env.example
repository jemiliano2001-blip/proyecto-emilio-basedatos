# Copia este archivo a .env.local y llena con los valores reales del
# proyecto de Supabase de Emilio. NUNCA subas .env.local a git.

NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu-anon-key-publica

# Solo servidor (Next server actions / Edge Functions). NUNCA con prefijo NEXT_PUBLIC_.
# Necesaria para /usuarios: crear cuentas Auth y resetear contraseñas.
# SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key-secreta
